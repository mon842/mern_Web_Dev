console.log('23');
// mathametical funtions 
let a=10;
let b=11;

let z=a+b;
console.log(z)


// math objects

w=Math;
console.log(w)
z=Math.E;//Euler's constant and the base of natural logarithms; approximately 2.718

z=Math.LN2//Natural logarithm of 2; approximately 0.693.

z=Math.LN10//Natural logarithm of 10; approximately 2.303.

z=Math.LOG2E//Base-2 logarithm of E; approximately 1.443.

z=Math.LOG10E//Base -10 logarithm of E; approximately 0.434

z=Math.PI//Ratio of a circle's circumference to its diameter; approximately 3.14159.

z=Math.SQRT1_2//Square root of ½; approximately 0.707

z=Math.SQRT2//square root of square root of 2

z=Math.abs(-10.22);//The absolute value of the given number

z=Math.acos(8/10)// Calculates angle of a right-angle triangle in radians

z=Math.floor(9.55);// function returns the largest integer less than or equal to a given number.

z=Math.ceil(9.55);// function always rounds a number up to the next largest integer.

z=Math.max(1,3,5,7,34);// function returns the largest of the zero or more numbers given as input parameters, or NaN if any parameter isn't a number and can't be converted into one

z=Math.min(1,3,5,7,34);// function returns the smallest of the zero or more numbers given as input parameters, or NaN if any parameter isn't a number and can't be converted into one

z=Math.log(5);// function returns the natural logarithm (base e) of a number

z=Math.pow(2,10);//function returns the base to the exponent power, as in base^exponent.

z=Math.random()*20;// function returns a random number between 0 and 20 or an integer between it

z=Math.round(1.11);// function returns the round value
console.log(z)

